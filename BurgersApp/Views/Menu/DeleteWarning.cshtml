@using BurgersApp.Application.ViewModel.Burgers
@model BurgerViewModel;

@{
    Layout = "_Layout";
}
<form asp-controller="Menu" asp-action="Delete" asp-route-id="@Model.Id" method="post">
    Are you sure you want to delete this item?
    <button type="submit" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Confirm</button>
    <a asp-controller="Menu" asp-action="Index" class="btn btn-secondary btn-lg active" role="button" aria-pressed="true">Return</a>
</form>